--- a/kometa.py
+++ b/kometa.py
@@
-default_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "config")
+def get_default_dir():
+    env_dir = os.environ.get("KOMETA_CONFIG_DIR")
+    if env_dir:
+        return os.path.expanduser(env_dir)
+    xdg_config_home = os.environ.get("XDG_CONFIG_HOME")
+    if xdg_config_home:
+        return os.path.join(xdg_config_home, "kometa")
+    return os.path.join(os.path.expanduser("~"), ".config", "kometa")
+
+default_dir = get_default_dir()
 load_dotenv(os.path.join(default_dir, ".env"))
@@
 elif not os.path.exists(os.path.join(default_dir, "config.yml")):
     git_branch = git_branch or "master"
     github_url = f"https://raw.githubusercontent.com/Kometa-Team/Kometa/{git_branch}/config/config.yml.template"
-    config_path = os.path.join(default_dir, "config.yml")
+    os.makedirs(default_dir, exist_ok=True)
+    config_path = os.path.join(default_dir, "config.yml")
